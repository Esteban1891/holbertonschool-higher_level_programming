#!/usr/bin/python3
def multiple_returns(sentence):
    if not sentence:
        First = None
        Length = 0
    else:
        Length = len(setence)
        First = setence[0]
        return (Length, First)
